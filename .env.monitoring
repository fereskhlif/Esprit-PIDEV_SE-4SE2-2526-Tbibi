# MySQL DSN for the MySQL Exporter
# Format: user:password@(host:port)/database
# If your MySQL has no password (root with empty pass):
MYSQL_DSN=root:@(host.docker.internal:3306)/tbibiii

# If your MySQL has a password, use:
# MYSQL_DSN=root:your_password@(host.docker.internal:3306)/tbibiii
